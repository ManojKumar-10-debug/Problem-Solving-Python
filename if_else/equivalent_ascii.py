# Notes:
# 1. Take an ASCII value as an integer input.
# 2. `chr(num)` converts the ASCII value into its equivalent character.
# 3. The converted character is stored in `ascii_char`.
# 4. Printing `ascii_char` displays the character represented by that ASCII value.

num = int(input())

ascii_char = chr(num)
print(ascii_char)